Config = {
    Position = 'top-right', -- posição padrão da UI (top-left, top-right)
    DefaultTimeout = 15000, -- tempo padrão para expiração de requests (em ms)
    AcceptKey = 'Y',        -- tecla padrão para aceitar requests
    DenyKey = 'N',          -- tecla padrão para recusar requests
}

return Config